# @onekeyfe/hardware-cli — DEPRECATED

> **⚠️ DEPRECATED**: Use the `onekey` CLI (`@onekeyfe/cli`) instead.
> `onekey --hardware` for wallet operations, `onekey device` for device management.
> This package will be removed in a future release.

Install: `npm install -g @onekeyfe/cli`
