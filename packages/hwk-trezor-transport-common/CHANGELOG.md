# Unreleased

- New package: split out of `@onekeyfe/hwk-trezor-transport`, mirroring upstream's
  `@trezor/transport` → `@trezor/transport` + `@trezor/transport-common` + `@trezor/transport-web`
  split as of v26.7.4. See `TREZOR_SOURCE.md`.
