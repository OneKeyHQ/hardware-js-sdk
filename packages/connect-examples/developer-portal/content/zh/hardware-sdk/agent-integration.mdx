---
title: AI Agent 集成
---

# AI Agent 集成

`@onekeyfe/hardware-cli` 使 AI Agent 能够通过 USB 与 OneKey 硬件钱包交互。

Skills 遵循 [Agent Skills](https://agentskills.io) 开放标准，已被 30+ 工具支持，包括 Claude Code、Codex、Gemini CLI、GitHub Copilot、Cursor、JetBrains 等。

## 安装

### Claude Code

```bash
claude plugin marketplace add OneKeyHQ/hardware-js-sdk --sparse .claude-plugin packages/hd-cli
claude plugin install onekey-hardware@onekey-hardware-plugins
```

CLI 会在首次使用时通过 skill 的 pre-flight 自动安装。

### Codex (OpenAI)

```bash
npm install -g @onekeyfe/hardware-cli

# 复制 skills 到 Codex skill 目录
git clone --depth 1 --filter=blob:none --sparse https://github.com/OneKeyHQ/hardware-js-sdk.git /tmp/onekey-hw-skills
cd /tmp/onekey-hw-skills && git sparse-checkout set packages/hd-cli/skills
cp -r packages/hd-cli/skills/device ~/.codex/skills/hardware-device
cp -r packages/hd-cli/skills/signing ~/.codex/skills/hardware-signing
cp -r packages/hd-cli/skills/firmware ~/.codex/skills/hardware-firmware
cp -r packages/hd-cli/skills/security ~/.codex/skills/hardware-security
rm -rf /tmp/onekey-hw-skills
```

### Gemini CLI

```bash
npm install -g @onekeyfe/hardware-cli

# 通过 Gemini 内置命令安装 skills
gemini skills install https://github.com/OneKeyHQ/hardware-js-sdk.git --path packages/hd-cli/skills/device
gemini skills install https://github.com/OneKeyHQ/hardware-js-sdk.git --path packages/hd-cli/skills/signing
gemini skills install https://github.com/OneKeyHQ/hardware-js-sdk.git --path packages/hd-cli/skills/firmware
gemini skills install https://github.com/OneKeyHQ/hardware-js-sdk.git --path packages/hd-cli/skills/security
```

### Cursor / Copilot / 其他

```bash
npm install -g @onekeyfe/hardware-cli
```

将 [skills/](https://github.com/OneKeyHQ/hardware-js-sdk/tree/onekey/packages/hd-cli/skills) 目录复制到你的项目中，或将 `AGENTS.md` 放到项目根目录。

## 技能

| 技能 | 关键命令 |
|------|----------|
| **设备** | `search`, `lock`, `device-verify`, `device-settings`, `device-wipe` |
| **签名**（27 条链） | `get-address`, `sign-transaction`, `sign-message`, `sign-typed-data`, `sign-psbt`, `batch-get-address` |
| **固件**（只读） | `firmware-check`, `firmware-check-all`, `bootloader-check` |
| **安全** | `change-pin`, `toggle-passphrase`, `passphrase-state` |

## 使用

```bash
# 搜索已连接的设备（自动获取设备信息）
onekey-hw search

# 获取以太坊地址
onekey-hw get-address --chain evm --use-empty-passphrase

# 获取比特币地址
onekey-hw get-address --chain btc --use-empty-passphrase

# 签名消息
onekey-hw sign-message --chain evm --message "hello" --use-empty-passphrase

# 签名 EVM 交易
onekey-hw sign-transaction --chain evm \
  --tx '{"to":"0x...","value":"0xf4240","chainId":1,"nonce":"0x0","gasLimit":"0x5208","gasPrice":"0xbebc200"}' \
  --use-empty-passphrase

# 检查固件
onekey-hw firmware-check-all
```

固件更新需通过 [OneKey App](https://onekey.so/download) 或 [firmware.onekey.so](https://firmware.onekey.so/)。

## 支持的链

| 类别 | 链 |
|------|-----|
| **EVM** | Ethereum, BSC, Polygon, Arbitrum, Base, Optimism, Avalanche 及所有 EVM 兼容链 |
| **Bitcoin 系** | Bitcoin (Legacy/SegWit/Taproot), Litecoin, Bitcoin Cash 及其他 BTC 分叉 |
| **Layer 1** | Solana, Cardano, Polkadot, Cosmos, Near, Aptos, Sui, TON, Stellar, XRP |
| **其他** | Tron, Filecoin, Kaspa, Algorand, Conflux, Nervos, Nostr 等 |

各链参数请参考[链方法文档](/zh/hardware-sdk/chains)。

## 输出格式

所有命令返回结构化 JSON：

```json
{
  "success": true,
  "payload": {
    "address": "0x1234...",
    "path": "m/44'/60'/0'/0/0"
  }
}
```

## API 映射

| CLI 命令 | SDK 方法 | 文档 |
|----------|---------|------|
| `get-address --chain evm` | `evmGetAddress` | [evmGetAddress](/zh/hardware-sdk/chains/ethereum-and-evm/evmgetaddress) |
| `get-address --chain btc` | `btcGetAddress` | [btcGetAddress](/zh/hardware-sdk/chains/bitcoin-and-bitcoin-forks/btcgetaddress) |
| `get-address --chain sol` | `solGetAddress` | [solGetAddress](/zh/hardware-sdk/chains/solana/solgetaddress) |
| `sign-transaction --chain evm` | `evmSignTransaction` | [evmSignTransaction](/zh/hardware-sdk/chains/ethereum-and-evm/evmsigntransaction) |
| `sign-transaction --chain btc` | `btcSignTransaction` | [btcSignTransaction](/zh/hardware-sdk/chains/bitcoin-and-bitcoin-forks/btcsigntransaction) |
| `sign-message --chain evm` | `evmSignMessage` | [evmSignMessage](/zh/hardware-sdk/chains/ethereum-and-evm/evmsignmessage) |
| `firmware-check` | `checkFirmwareRelease` | [checkFirmwareRelease](/zh/hardware-sdk/device-api/checkfirmwarerelease) |
| `device-verify` | `deviceVerify` | [deviceVerify](/zh/hardware-sdk/device-api/deviceverify) |
| `change-pin` | `deviceChangePin` | [deviceChangePin](/zh/hardware-sdk/device-api/devicechangepin) |
