---
title: deviceVerify
---

# deviceVerify

## 验证设备

从设备获取验证挑战对应的 `cert` 和 `signature`。由宿主将结果提交给验证服务；此 API 不执行服务器验证。读取签名数据需要在设备上确认。

```typescript
const result = await HardwareSDK.deviceVerify(connectId, params);
```

## 参数

[**可选通用参数**](/zh/hardware-sdk/concepts/common-params)


* `dataHex` - _必需_ `string` 验证流程提供的非空 hex 编码挑战数据。

## 示例

```typescript
// Obtain a fresh challenge from your verification flow.
declare const challengeHex: string;

HardwareSDK.deviceVerify(connectId, {
    dataHex: challengeHex
});
```

## 返回结果

```typescript
{
    success: true,
    payload: {
        cert: string
        signature: string
    }
}
```

错误

```typescript
{
    success: false,
    payload: {
        error: string, // 错误信息
        code?: string | number // 错误码
    }
}
```
