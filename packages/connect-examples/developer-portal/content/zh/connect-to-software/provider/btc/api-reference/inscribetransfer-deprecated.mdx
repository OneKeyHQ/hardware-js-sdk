---
title: inscribeTransfer (已弃用)
---

# inscribeTransfer (已弃用)

铭刻转账

### 方法

```typescript
async function inscribeTransfer(
    ticker: string,
    amount: string,
): string
```

### 参数

* `ticker` — _必填_ `string` 代币符号
* `amount` — _必填_ `string` 数量

### 响应

`txid` — `string`

### 示例

```typescript
const provider = (window.$onekey && window.$onekey.btc) || window.unisat;

const ticker = "010203"
const amount = "100"
const txid = async provider.signPsbt(psbtHex);

```

