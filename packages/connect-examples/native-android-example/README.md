# Hardware lowlevel communicate (Android)

Sample project for OneKey hardware wallet's low-level BLE communication.

Uses Hardware SDK **1.2.x** (`getDeviceState`, Protocol V1 and V2). After changing `web/`, rebuild the embedded WebView bundle:

```bash
cd web
yarn
yarn build
```
