---
title: pushPsbt
---

# pushPsbt

推送交易

### 方法

```typescript
async function pushPsbt(psbtHex: string): string
```

### 参数

* `rawtx` — _必填_ `string` 要推送的 PSBT 十六进制字符串

### 响应

`txid` — `string`

### 示例

```typescript
const provider = (window.$onekey && window.$onekey.btc) || window.unisat;

const psbtHex = "010203"
const txid = async provider.pushPsbt(psbtHex);

```

### 演示

<iframe src="https://codepen.io/OneKeyHQ/pen/dyaqzgE" width="100%" height="400" frameBorder="0" allowFullScreen></iframe>

